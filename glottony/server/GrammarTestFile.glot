struct Foo(name: any, count: int)

fun bar(input: Foo): string = input.data + ", " input.count

fun b(): boolean = {
    val name = 5
    return true
}

fun baz(input: Foo): string = {
    val v = input.name as string
    return input.name
}